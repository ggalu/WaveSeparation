"""
The wave-separation library: the separation itself (wave_separation.py), the
attenuation fit, and the case-folder plumbing (config, cases, experiment, dump)
every script in 3wave/ imports. Nothing here is run directly.
"""
